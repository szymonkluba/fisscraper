import { RemovePrefixPipe } from './remove-prefix.pipe';

describe('RemovePrefixPipe', () => {
  it('create an instance', () => {
    const pipe = new RemovePrefixPipe();
    expect(pipe).toBeTruthy();
  });
});
