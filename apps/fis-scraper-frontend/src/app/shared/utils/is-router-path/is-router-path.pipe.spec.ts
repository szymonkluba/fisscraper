import { IsRouterPathPipe } from './is-router-path.pipe';

describe('IsRouterPathPipe', () => {
  it('create an instance', () => {
    const pipe = new IsRouterPathPipe();
    expect(pipe).toBeTruthy();
  });
});
