import { NestedValuePipe } from './nested-value.pipe';

describe('NestedValuePipe', () => {
  it('create an instance', () => {
    const pipe = new NestedValuePipe();
    expect(pipe).toBeTruthy();
  });
});
